import createAutoSizer from './components/createAutoSizer'

/**
 * react-autosizer
 * Takes object with dependencies (React) and exports a component that controls a target(s) heighth/widths, based on a source height / width.
 * @return {Object}              <AutoSizer /> Component Factory
 */
export default createAutoSizer
