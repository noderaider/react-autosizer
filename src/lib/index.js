import reactAutosizer from './components/react-autosizer'

/**
 * react-autosizer
 * Requires dependencies { React, [connect] } and exports a component that controls a target(s) heighth/widths, based on a source height / width.
 * @param  {[type]} dependencies [description]
 * @return {[type]}              <Autosizer /> Component Factory
 */
export default reactAutosizer
